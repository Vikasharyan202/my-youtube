import SidebarSlice from "../utils/sidebarSlice";
import SideBar from "./SideBar";

const Body = () => {
    return(
        <div className="body_count">
            <SideBar />
        </div>
    )
}

export default Body;